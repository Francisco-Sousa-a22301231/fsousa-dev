---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Criação de Websites | Francisco Sousa',
  description = 'Websites rápidos e modernos para negócios. Páginas web a partir de €300. Entrega em 5 dias. Peça orçamento grátis.',
} = Astro.props;

const canonicalUrl = 'https://fsousa-dev.pages.dev';
const ogImage = `${canonicalUrl}/og-image.png`;

const faqData = [
  { question: "Quanto custa por mês depois?", answer: "Zero. O alojamento do primeiro ano está incluído. Depois disso, o custo de alojamento é menos de €10/mês — e eu ajudo a configurar tudo. Não há mensalidades pelo meu trabalho, nunca." },
  { question: "Quanto tempo demora?", answer: "Uma página simples fica pronta em 3–5 dias úteis. Um website completo em 1–2 semanas." },
  { question: "Já tenho Instagram, para quê um site?", answer: "O Instagram não aparece no Google. Quando alguém pesquisa o seu tipo de negócio, encontra quem tem site — não quem tem Instagram." },
  { question: "Preciso de domínio e alojamento?", answer: "Eu trato de tudo. O custo de alojamento está incluído no primeiro ano." },
  { question: "Posso alterar o conteúdo sozinho?", answer: "Sim. Incluo um sistema simples para editar textos e imagens sem precisar de programar." },
  { question: "Posso ver exemplos antes de começar?", answer: "Sim. Antes de começar a construir, mostro-lhe um esboço do design para aprovação. Só avanço quando estiver satisfeito." },
  { question: "Trabalha remotamente?", answer: "Sim. Trabalho remotamente para qualquer parte do mundo. Reuniões por WhatsApp ou videochamada — funciona perfeitamente." },
  { question: "E se não gostar do resultado?", answer: "Mostro-lhe o progresso a cada etapa. Só paga o segundo pagamento quando estiver 100% satisfeito. Sem risco." },
];
---

<!doctype html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Fonts self-hosted via @fontsource — no external preconnects needed -->
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:locale" content="pt_PT" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <!-- LocalBusiness structured data -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "ProfessionalService",
      "name": "Francisco Sousa — Web Developer",
      "description": "Criação de websites rápidos e modernos para negócios.",
      "url": "https://fsousa-dev.pages.dev",
      "telephone": "+351939021876",
      "email": "franciscobarrososousa@gmail.com",
      "address": {
        "@type": "PostalAddress",
        "addressLocality": "Cascais",
        "addressCountry": "PT"
      },
      "priceRange": "€€",
      "openingHours": "Mo-Fr 09:00-18:00",
      "sameAs": ["https://github.com/Francisco-Sousa-a22301231"]
    })} />

    <!-- FAQPage structured data -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": faqData.map(faq => ({
        "@type": "Question",
        "name": faq.question,
        "acceptedAnswer": {
          "@type": "Answer",
          "text": faq.answer
        }
      }))
    })} />
  </head>
  <body class="bg-[#0f172a] text-[#0f172a]">

    <!-- NAVBAR -->
    <header id="main-header" class="fixed top-0 left-0 right-0 z-50 bg-transparent border-b border-transparent transition-all duration-500">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 flex items-center justify-between h-16">
        <!-- Logo -->
        <a href="/" class="flex items-center gap-2 group">
          <span class="text-[#f97316] font-bold text-xl" style="font-family:'Sora',sans-serif;">FS</span>
          <span class="text-white font-semibold text-sm hidden sm:block" style="font-family:'Sora',sans-serif;">Francisco Sousa</span>
        </a>

        <!-- Desktop nav -->
        <nav class="hidden md:flex items-center gap-6">
          <a href="#servicos" class="nav-link text-white/80 hover:text-[#f97316] text-sm font-medium transition-colors" data-section="servicos">Serviços</a>
          <a href="#processo" class="nav-link text-white/80 hover:text-[#f97316] text-sm font-medium transition-colors" data-section="processo">Processo</a>
          <a href="#portfolio" class="nav-link text-white/80 hover:text-[#f97316] text-sm font-medium transition-colors" data-section="portfolio">Portfolio</a>
          <a href="#contacto-form" class="nav-link text-white/80 hover:text-[#f97316] text-sm font-medium transition-colors" data-section="contacto-form">Contacto</a>

          <!-- Phone number -->
          <a href="tel:+351939021876" class="flex items-center gap-1.5 text-white/60 hover:text-white text-sm font-medium transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.36 11.9 19.79 19.79 0 0 1 1.27 3.3 2 2 0 0 1 3.24 1h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.1a16 16 0 0 0 6 6l.62-.58a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 21.28 16h.72z"/></svg>
            939 021 876
          </a>

        </nav>

        <!-- Mobile hamburger -->
        <button
          id="menu-btn"
          class="md:hidden text-white p-2 rounded-lg hover:bg-white/10 transition-colors"
          aria-label="Abrir menu"
          aria-expanded="false"
        >
          <svg id="icon-open" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="3" y1="6" x2="21" y2="6"/>
            <line x1="3" y1="12" x2="21" y2="12"/>
            <line x1="3" y1="18" x2="21" y2="18"/>
          </svg>
          <svg id="icon-close" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="hidden">
            <line x1="18" y1="6" x2="6" y2="18"/>
            <line x1="6" y1="6" x2="18" y2="18"/>
          </svg>
        </button>
      </div>

      <!-- Mobile menu slide-in -->
      <div
        id="mobile-menu"
        class="md:hidden overflow-hidden max-h-0 transition-all duration-300 ease-in-out bg-[#0f172a] border-t border-white/10"
      >
        <nav class="flex flex-col px-4 py-4 gap-1">
          <a href="#servicos" class="mobile-link text-white/80 hover:text-[#f97316] text-base font-medium py-3 px-2 rounded-lg hover:bg-white/5 transition-colors">Serviços</a>
          <a href="#processo" class="mobile-link text-white/80 hover:text-[#f97316] text-base font-medium py-3 px-2 rounded-lg hover:bg-white/5 transition-colors">Processo</a>
          <a href="#portfolio" class="mobile-link text-white/80 hover:text-[#f97316] text-base font-medium py-3 px-2 rounded-lg hover:bg-white/5 transition-colors">Portfolio</a>
          <a href="#contacto-form" class="mobile-link text-white/80 hover:text-[#f97316] text-base font-medium py-3 px-2 rounded-lg hover:bg-white/5 transition-colors">Contacto</a>

          <!-- Phone number (mobile) -->
          <a href="tel:+351939021876" class="flex items-center gap-2 text-white/70 text-base font-medium py-3 px-2 rounded-lg hover:bg-white/5 transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.36 11.9 19.79 19.79 0 0 1 1.27 3.3 2 2 0 0 1 3.24 1h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.1a16 16 0 0 0 6 6l.62-.58a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 21.28 16h.72z"/></svg>
            939 021 876
          </a>

        </nav>
      </div>
    </header>

    <!-- PAGE CONTENT -->
    <main class="pt-16">
      <slot />
    </main>

    <!-- FOOTER -->
    <footer id="contacto" class="bg-[#0f172a] text-white pt-0 pb-6">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 pt-10">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 pb-8 border-b border-white/10">
          <!-- Brand -->
          <div>
            <div class="flex items-center gap-2 mb-3">
              <span class="text-[#f97316] font-bold text-2xl" style="font-family:'Sora',sans-serif;">FS</span>
              <span class="font-semibold text-lg" style="font-family:'Sora',sans-serif;">Francisco Sousa</span>
            </div>
            <p class="text-white/60 text-sm leading-relaxed">Websites rápidos e modernos para negócios. Trabalho remotamente para todo o mundo.</p>
          </div>
          <!-- Links -->
          <div>
            <h4 class="font-semibold text-sm uppercase tracking-wider text-white/40 mb-4">Navegação</h4>
            <ul class="space-y-2 text-sm text-white/70">
              <li><a href="#servicos" class="hover:text-[#f97316] transition-colors">Serviços</a></li>
              <li><a href="#processo" class="hover:text-[#f97316] transition-colors">Processo</a></li>
              <li><a href="#portfolio" class="hover:text-[#f97316] transition-colors">Portfolio</a></li>
              <li><a href="#contacto-form" class="hover:text-[#f97316] transition-colors">Contacto</a></li>
            </ul>
          </div>
          <!-- Contact -->
          <div>
            <h4 class="font-semibold text-sm uppercase tracking-wider text-white/40 mb-4">Contacto</h4>
            <ul class="space-y-1 text-sm">
              <li>
                <a href="tel:+351939021876" class="flex items-center gap-2 py-2 text-white/70 hover:text-[#f97316] transition-colors">
                  <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.36 11.9 19.79 19.79 0 0 1 1.27 3.3 2 2 0 0 1 3.24 1h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.1a16 16 0 0 0 6 6l.62-.58a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 21.28 16h.72z"/></svg>
                  939 021 876
                </a>
              </li>
              <li>
                <a
                  href="https://wa.me/351939021876?text=Ol%C3%A1%20Francisco%2C%20vi%20o%20seu%20site%20e%20estou%20interessado%20num%20or%C3%A7amento%20para%20o%20meu%20neg%C3%B3cio."
                  target="_blank"
                  rel="noopener noreferrer"
                  class="flex items-center gap-2 py-2 text-[#25d366] hover:text-[#1fbe5a] font-semibold transition-colors"
                >
                  <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
                  </svg>
                  WhatsApp
                </a>
              </li>
              <li>
                <a href="mailto:franciscobarrososousa@gmail.com" class="flex items-center gap-2 py-2 text-white/70 hover:text-[#f97316] transition-colors">
                  <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="2" y="4" width="20" height="16" rx="2"/>
                    <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
                  </svg>
                  franciscobarrososousa@gmail.com
                </a>
              </li>
              <li class="flex items-center gap-2 text-white/60">
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <circle cx="12" cy="12" r="10"/><path d="M2 12h20M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
                </svg>
                From Portugal 🇵🇹
              </li>
            </ul>
          </div>
        </div>
        <div class="pt-6 flex flex-col sm:flex-row items-center justify-between gap-2 text-sm text-white/40">
          <p>&copy; {new Date().getFullYear()} Francisco Sousa. Todos os direitos reservados.</p>
          <p>Feito com Astro e Tailwind CSS</p>
        </div>
      </div>
    </footer>

    <!-- ===== LOADING SCREEN ===== -->
    <div id="loading-screen" class="fixed inset-0 z-[200] flex items-center justify-center bg-[#0f172a] overflow-hidden">
      <!-- Scan line effect -->
      <div class="loader-scan"></div>

      <!-- Orbit rings with particles -->
      <div class="orbit-ring-1 flex items-start justify-center" style="margin-top:-80px;margin-left:-80px;top:50%;left:50%;">
        <div class="loader-orbit" style="margin-top:-3px;margin-left:-3px;"></div>
      </div>
      <div class="orbit-ring-2 flex items-end justify-end" style="margin-top:-100px;margin-left:-100px;top:50%;left:50%;">
        <div class="loader-orbit loader-orbit-2" style="margin-bottom:-3px;margin-right:-3px;"></div>
      </div>
      <div class="orbit-ring-1 flex items-end justify-start" style="margin-top:-80px;margin-left:-80px;top:50%;left:50%;animation-delay:1s;animation-duration:4s;">
        <div class="loader-orbit loader-orbit-3" style="margin-bottom:-2px;margin-left:-2px;"></div>
      </div>

      <!-- Center content -->
      <div class="relative z-10 flex flex-col items-center gap-5">
        <!-- SVG ring + logo -->
        <div class="relative flex items-center justify-center">
          <svg width="120" height="120" viewBox="0 0 120 120" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle class="loader-ring" cx="60" cy="60" r="45" stroke="#f97316" stroke-width="1.5" stroke-linecap="round"/>
            <circle cx="60" cy="60" r="45" stroke="white" stroke-width="1" opacity="0.06"/>
          </svg>
          <div class="loader-fs absolute inset-0 flex items-center justify-center">
            <span class="text-3xl font-extrabold" style="font-family:'Sora',sans-serif;background:linear-gradient(135deg,#f97316,#fb923c,#f59e0b);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;">FS</span>
          </div>
        </div>

        <!-- Tagline -->
        <p class="loader-tagline text-white text-xs uppercase tracking-[0.4em]">Francisco Sousa</p>
      </div>
    </div>

    <!-- Floating WhatsApp Button (desktop only) -->
    <a
      href="https://wa.me/351939021876?text=Ol%C3%A1%20Francisco%2C%20vi%20o%20seu%20site%20e%20estou%20interessado%20num%20or%C3%A7amento%20para%20o%20meu%20neg%C3%B3cio."
      target="_blank"
      rel="noopener noreferrer"
      class="fixed bottom-6 right-6 z-50 bg-[#25d366] hover:bg-[#1fbe5a] text-white w-14 h-14 rounded-full hidden md:flex items-center justify-center shadow-lg hover:shadow-xl hover:scale-110 transition-all duration-300"
      aria-label="Contactar via WhatsApp"
    >
      <svg class="relative z-10" xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="currentColor">
        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
      </svg>
    </a>


    <script>
      // ===== LOADING SCREEN DISMISS =====
      const loadingScreen = document.getElementById('loading-screen');
      if (loadingScreen) {
        // Dismiss after 2.2s (animation finishes at ~1.7s)
        setTimeout(() => {
          loadingScreen.classList.add('loading-done');
          setTimeout(() => { loadingScreen.remove(); }, 750);
        }, 1900);
      }
    </script>

    <script>
      import { gsap } from 'gsap';
      import { ScrollTrigger } from 'gsap/ScrollTrigger';
      import Lenis from 'lenis';

      gsap.registerPlugin(ScrollTrigger);

      // ===== REDUCED MOTION CHECK =====
      const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      // ===== LENIS SMOOTH SCROLL =====
      if (!prefersReducedMotion) {
        const lenis = new Lenis({
          duration: 1.2,
          easing: (t: number) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
          touchMultiplier: 2,
        });

        // Sync Lenis with GSAP ticker
        lenis.on('scroll', ScrollTrigger.update);
        gsap.ticker.add((time: number) => {
          lenis.raf(time * 1000);
        });
        gsap.ticker.lagSmoothing(0);
      }

      // ===== MOBILE MENU =====
      const btn = document.getElementById('menu-btn');
      const menu = document.getElementById('mobile-menu');
      const iconOpen = document.getElementById('icon-open');
      const iconClose = document.getElementById('icon-close');

      btn?.addEventListener('click', () => {
        const isOpen = menu!.style.maxHeight && menu!.style.maxHeight !== '0px';
        if (isOpen) {
          menu!.style.maxHeight = '0px';
          iconOpen!.classList.remove('hidden');
          iconClose!.classList.add('hidden');
          btn!.setAttribute('aria-expanded', 'false');
        } else {
          menu!.style.maxHeight = menu!.scrollHeight + 'px';
          iconOpen!.classList.add('hidden');
          iconClose!.classList.remove('hidden');
          btn!.setAttribute('aria-expanded', 'true');
        }
      });

      document.querySelectorAll('.mobile-link').forEach(link => {
        link.addEventListener('click', () => {
          menu!.style.maxHeight = '0px';
          iconOpen!.classList.remove('hidden');
          iconClose!.classList.add('hidden');
          btn!.setAttribute('aria-expanded', 'false');
        });
      });

      // ===== NAV: TRANSPARENT → GLASS ON SCROLL =====
      const header = document.getElementById('main-header');
      const heroSection = document.getElementById('hero');
      if (header && heroSection) {
        const headerObserver = new IntersectionObserver(
          ([entry]) => {
            if (!entry.isIntersecting) {
              header.classList.add('nav-glass');
            } else {
              header.classList.remove('nav-glass');
            }
          },
          { threshold: 0, rootMargin: '-64px 0px 0px 0px' }
        );
        headerObserver.observe(heroSection);
      }

      // ===== NAV: ACTIVE SECTION INDICATOR =====
      const navLinks = document.querySelectorAll<HTMLAnchorElement>('.nav-link[data-section]');
      const sections = document.querySelectorAll<HTMLElement>('section[id]');

      if (sections.length > 0 && navLinks.length > 0) {
        const sectionObserver = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                const id = entry.target.id;
                navLinks.forEach((link) => {
                  link.classList.toggle('active', link.dataset.section === id);
                });
              }
            });
          },
          { threshold: 0.3, rootMargin: '-80px 0px -50% 0px' }
        );
        sections.forEach((section) => sectionObserver.observe(section));
      }

      // ===== GSAP SCROLL ANIMATIONS =====
      if (!prefersReducedMotion) {
        // Batch scroll reveal for [data-animate] elements
        gsap.utils.toArray<HTMLElement>('[data-animate]').forEach((el) => {
          const delay = parseFloat(el.dataset.delay || '0') * 0.1;
          gsap.fromTo(el,
            { opacity: 0, y: 24 },
            {
              opacity: 1,
              y: 0,
              duration: 0.7,
              delay,
              ease: 'power2.out',
              scrollTrigger: {
                trigger: el,
                start: 'top 85%',
                once: true,
              },
            }
          );
        });

        // Comparison table: slide columns from opposite sides
        gsap.utils.toArray<HTMLElement>('[data-animate-left]').forEach((el) => {
          gsap.fromTo(el,
            { opacity: 0, x: -30 },
            {
              opacity: 1,
              x: 0,
              duration: 0.7,
              ease: 'power2.out',
              scrollTrigger: { trigger: el, start: 'top 85%', once: true },
            }
          );
        });

        gsap.utils.toArray<HTMLElement>('[data-animate-right]').forEach((el) => {
          gsap.fromTo(el,
            { opacity: 0, x: 30 },
            {
              opacity: 1,
              x: 0,
              duration: 0.7,
              ease: 'power2.out',
              scrollTrigger: { trigger: el, start: 'top 85%', once: true },
            }
          );
        });

        // Process timeline: line draws on scroll
        const timelineLine = document.querySelector<HTMLElement>('.timeline-progress');
        const timelineContainer = document.getElementById('processo');
        if (timelineLine && timelineContainer) {
          gsap.fromTo(timelineLine,
            { width: '0%' },
            {
              width: '100%',
              ease: 'none',
              scrollTrigger: {
                trigger: timelineContainer,
                start: 'top 60%',
                end: 'bottom 40%',
                scrub: 1,
              },
            }
          );
        }

        // Pricing cards stagger
        const pricingCards = document.querySelectorAll('.pricing-card');
        if (pricingCards.length > 0) {
          gsap.fromTo(pricingCards,
            { opacity: 0, y: 40 },
            {
              opacity: 1,
              y: 0,
              duration: 0.7,
              stagger: 0.15,
              ease: 'power2.out',
              scrollTrigger: {
                trigger: pricingCards[0],
                start: 'top 85%',
                once: true,
              },
            }
          );
        }
      } else {
        // Reduced motion: show everything immediately
        document.querySelectorAll('[data-animate], [data-animate-left], [data-animate-right]').forEach((el) => {
          (el as HTMLElement).style.opacity = '1';
          (el as HTMLElement).style.transform = 'none';
        });
      }

      // ===== MAGNETIC BUTTON EFFECT (desktop only) =====
      if (!prefersReducedMotion && window.matchMedia('(pointer: fine)').matches) {
        document.querySelectorAll('.magnetic-btn').forEach((btn) => {
          const el = btn as HTMLElement;
          const strength = 0.3;

          el.addEventListener('mousemove', (e: MouseEvent) => {
            const rect = el.getBoundingClientRect();
            const x = e.clientX - rect.left - rect.width / 2;
            const y = e.clientY - rect.top - rect.height / 2;
            el.style.transform = `translate(${x * strength}px, ${y * strength}px)`;
          });

          el.addEventListener('mouseleave', () => {
            el.style.transform = 'translate(0, 0)';
          });
        });
      }

      // ===== COUNTER ANIMATION (keep existing, works well) =====
      const counterObserver = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const el = entry.target as HTMLElement;
            const target = parseInt(el.dataset.count || '0', 10);
            const prefix = el.dataset.prefix || '';
            const suffix = el.dataset.suffix || '';
            const duration = prefersReducedMotion ? 0 : 1500;
            const start = performance.now();

            if (prefersReducedMotion) {
              el.textContent = prefix + target + suffix;
              counterObserver.unobserve(el);
              return;
            }

            function animate(now: number) {
              const elapsed = now - start;
              const progress = Math.min(elapsed / duration, 1);
              const eased = 1 - Math.pow(1 - progress, 3);
              const current = Math.round(eased * target);
              el.textContent = prefix + current + suffix;
              if (progress < 1) requestAnimationFrame(animate);
            }

            requestAnimationFrame(animate);
            counterObserver.unobserve(el);
          }
        });
      }, { threshold: 0.5 });

      document.querySelectorAll('[data-count]').forEach((el) => {
        counterObserver.observe(el);
      });
    </script>
  </body>
</html>
